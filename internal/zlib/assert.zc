import bool

func eq /a /b
	if /a /b bool.neq
		'assertion failed: `a` == `b`'  abort

func true /a
	if /a bool.true bool.neq
		'assertion failed: not true' abort

func false /a
	if /a bool.false bool.neq
		'assertion failed: not false' abort
