native and

macro false 'false'
alias f false

native gt

native gte

func iif /expr /tval /fval
    if /expr
        /tval
    else
        /fval

macro true 'true'
alias t true

native lt

native lte

native neq

native not

native or
