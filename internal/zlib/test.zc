func run //files
	for file; files
		import testing; file 
		globals; testing globals
		for global in globals
			if /global 'test-' starts-with
				try /name eval
				if neq
					err; pop
					'FAIL: `name`: `err`' print
				else
					'pass: /name'
