import assert
import io

func test-for-scope
    for x; 1 2
        -- temp stack should start empty at the top of each loop
        temp; 3
        temp; n 1 assert.eq

func for-break
    for x; 1 2 3 4
        if /x 3 eq
            /x
            return

func test-for-break
    for-break
    3 assert.eq